# Planification de séance + notifications push — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Planifier sa semaine (un type de séance par jour), recevoir un push le jour J (app fermée), et arriver sur une séance pré-configurée sans bloquer le reste.

**Architecture:** Tables Supabase `planned_sessions` + `push_subscriptions` (RLS via JWT Clerk). UI greffée sur l'existant : chip dans `StatusSpot`, `PlanWeekModal`, split-button jour J dans `IdleScreen`, pré-sélection via `NavContext.plannedType`. Web Push (VAPID) avec handlers dans `public/sw.js`. Envoi quotidien par un Vercel Cron (`/api/cron/notify`) utilisant un client admin Supabase (clé secrète, bypass RLS).

**Tech Stack:** Next.js 16 (App Router), React 19, Supabase JS, Clerk, `web-push`, Vitest, Vercel Cron.

---

> **⚠️ AGENTS.md :** ce Next.js a des breaking changes. Avant d'écrire une route handler ou de la config runtime/cron, lire les guides concernés dans `node_modules/next/dist/docs/` (route handlers, `runtime`, et la doc Vercel Cron). Ne pas se fier à la mémoire d'entraînement.

> **Répertoire de travail :** toutes les commandes s'exécutent depuis `C:\Users\yassi\Desktop\tcp\t-aspriscombien` (la racine git).

---

## Palier A — Planification persistante (DB + logique + API)

### Task 1: Migration SQL `planned_sessions` + `push_subscriptions`

**Files:**
- Create: `supabase/migrations/20260602_planning_push.sql`

- [ ] **Step 1: Écrire la migration**

```sql
-- Planification de séance + abonnements Web Push.
-- À exécuter dans l'éditeur SQL Supabase (convention du projet : migrations manuelles).

-- Séances planifiées : 1 type par jour et par utilisateur.
CREATE TABLE IF NOT EXISTS public.planned_sessions (
  id         bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id    text NOT NULL DEFAULT (auth.jwt() ->> 'sub'),
  date       date NOT NULL,
  type       text NOT NULL CHECK (type IN ('push','pull','legs','full','upper','core','athletics')),
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (user_id, date)
);

ALTER TABLE public.planned_sessions ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS planned_select_own ON public.planned_sessions;
DROP POLICY IF EXISTS planned_insert_own ON public.planned_sessions;
DROP POLICY IF EXISTS planned_update_own ON public.planned_sessions;
DROP POLICY IF EXISTS planned_delete_own ON public.planned_sessions;
CREATE POLICY planned_select_own ON public.planned_sessions
  FOR SELECT USING (user_id = (auth.jwt() ->> 'sub'));
CREATE POLICY planned_insert_own ON public.planned_sessions
  FOR INSERT WITH CHECK (user_id = (auth.jwt() ->> 'sub'));
CREATE POLICY planned_update_own ON public.planned_sessions
  FOR UPDATE USING (user_id = (auth.jwt() ->> 'sub'))
             WITH CHECK (user_id = (auth.jwt() ->> 'sub'));
CREATE POLICY planned_delete_own ON public.planned_sessions
  FOR DELETE USING (user_id = (auth.jwt() ->> 'sub'));

-- Abonnements Web Push (1 par appareil/navigateur).
CREATE TABLE IF NOT EXISTS public.push_subscriptions (
  id         bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id    text NOT NULL DEFAULT (auth.jwt() ->> 'sub'),
  endpoint   text NOT NULL UNIQUE,
  p256dh     text NOT NULL,
  auth       text NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);

ALTER TABLE public.push_subscriptions ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS push_select_own ON public.push_subscriptions;
DROP POLICY IF EXISTS push_insert_own ON public.push_subscriptions;
DROP POLICY IF EXISTS push_delete_own ON public.push_subscriptions;
CREATE POLICY push_select_own ON public.push_subscriptions
  FOR SELECT USING (user_id = (auth.jwt() ->> 'sub'));
CREATE POLICY push_insert_own ON public.push_subscriptions
  FOR INSERT WITH CHECK (user_id = (auth.jwt() ->> 'sub'));
CREATE POLICY push_delete_own ON public.push_subscriptions
  FOR DELETE USING (user_id = (auth.jwt() ->> 'sub'));
```

- [ ] **Step 2: Appliquer la migration**

Coller le SQL dans l'éditeur SQL Supabase et exécuter (ou via MCP Supabase `apply_migration`). Vérifier que les 2 tables apparaissent dans la liste des tables.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20260602_planning_push.sql
git commit -m "feat(db): planned_sessions + push_subscriptions tables"
```

---

### Task 2: Helpers de date (`isoToday`, `isoDateInParis`, `weekDatesFrom`)

**Files:**
- Modify: `app/seance/_lib/helpers.ts` (ajout en fin de fichier)
- Test: `app/seance/_lib/plan-dates.test.ts`

- [ ] **Step 1: Écrire le test qui échoue**

```ts
// app/seance/_lib/plan-dates.test.ts
import { describe, expect, it } from 'vitest'
import { isoDateInParis, weekDatesFrom } from './helpers'

describe('isoDateInParis', () => {
  it('rend la date locale Europe/Paris au format YYYY-MM-DD', () => {
    // 2026-06-02T23:30:00Z = 01:30 le 3 juin à Paris (CEST, UTC+2)
    expect(isoDateInParis(new Date('2026-06-02T23:30:00Z'))).toBe('2026-06-03')
    // 2026-01-15T00:30:00Z = 01:30 le 15 janvier à Paris (CET, UTC+1)
    expect(isoDateInParis(new Date('2026-01-15T00:30:00Z'))).toBe('2026-01-15')
  })
})

describe('weekDatesFrom', () => {
  it('rend les 7 jours ISO à partir du lundi donné', () => {
    expect(weekDatesFrom('2026-06-08')).toEqual([
      '2026-06-08', '2026-06-09', '2026-06-10', '2026-06-11',
      '2026-06-12', '2026-06-13', '2026-06-14',
    ])
  })
})
```

- [ ] **Step 2: Lancer le test, vérifier l'échec**

Run: `npm test -- plan-dates`
Expected: FAIL (`isoDateInParis is not a function`).

- [ ] **Step 3: Implémenter les helpers**

Ajouter à la fin de `app/seance/_lib/helpers.ts` :

```ts
// Date du jour au fuseau Europe/Paris, format YYYY-MM-DD (en-CA = ISO).
export function isoDateInParis(now: Date): string {
  return new Intl.DateTimeFormat('en-CA', {
    timeZone: 'Europe/Paris',
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
  }).format(now)
}

// Les 7 dates ISO (YYYY-MM-DD) d'une semaine à partir de son lundi.
export function weekDatesFrom(mondayIso: string): string[] {
  const base = new Date(mondayIso + 'T00:00:00Z')
  return Array.from({ length: 7 }, (_, i) => {
    const d = new Date(base)
    d.setUTCDate(d.getUTCDate() + i)
    return d.toISOString().slice(0, 10)
  })
}
```

- [ ] **Step 4: Lancer le test, vérifier le succès**

Run: `npm test -- plan-dates`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/seance/_lib/helpers.ts app/seance/_lib/plan-dates.test.ts
git commit -m "feat(plan): date helpers isoDateInParis + weekDatesFrom"
```

---

### Task 3: Logique pure `planDiff` (upserts/deletes du batch)

**Files:**
- Create: `app/seance/_lib/plan.ts`
- Test: `app/seance/_lib/plan.test.ts`

- [ ] **Step 1: Écrire le test qui échoue**

```ts
// app/seance/_lib/plan.test.ts
import { describe, expect, it } from 'vitest'
import { planDiff } from './plan'

const WEEK = ['2026-06-08','2026-06-09','2026-06-10','2026-06-11','2026-06-12','2026-06-13','2026-06-14']

describe('planDiff', () => {
  it('détecte ajouts, changements et suppressions dans la semaine', () => {
    const existing = [
      { date: '2026-06-08', type: 'push' },
      { date: '2026-06-10', type: 'pull' },
    ]
    const edited = [
      { date: '2026-06-08', type: 'push' }, // inchangé → ni upsert ni delete
      { date: '2026-06-10', type: 'legs' }, // changé → upsert
      { date: '2026-06-12', type: 'core' }, // nouveau → upsert
      // 2026-06-09 absent partout → rien
      // 2026-06-10 existait, reste → ok ; aucune suppression ici
    ]
    const { upserts, deletes } = planDiff(WEEK, existing, edited)
    expect(upserts).toEqual([
      { date: '2026-06-10', type: 'legs' },
      { date: '2026-06-12', type: 'core' },
    ])
    expect(deletes).toEqual([])
  })

  it('supprime un jour retiré de la semaine', () => {
    const existing = [{ date: '2026-06-08', type: 'push' }]
    const edited: { date: string; type: string }[] = []
    const { upserts, deletes } = planDiff(WEEK, existing, edited)
    expect(upserts).toEqual([])
    expect(deletes).toEqual(['2026-06-08'])
  })

  it('ignore les dates hors de la semaine', () => {
    const { upserts, deletes } = planDiff(
      WEEK,
      [],
      [{ date: '2026-07-01', type: 'push' }],
    )
    expect(upserts).toEqual([])
    expect(deletes).toEqual([])
  })
})
```

- [ ] **Step 2: Lancer le test, vérifier l'échec**

Run: `npm test -- plan.test`
Expected: FAIL (module `./plan` introuvable).

- [ ] **Step 3: Implémenter `planDiff`**

```ts
// app/seance/_lib/plan.ts
export type PlanEntry = { date: string; type: string }

// Compare l'état édité au stocké, restreint à la semaine, et rend les écritures
// minimales : upserts (jours ajoutés/changés) + deletes (jours retirés).
export function planDiff(
  weekDates: string[],
  existing: PlanEntry[],
  edited: PlanEntry[],
): { upserts: PlanEntry[]; deletes: string[] } {
  const week = new Set(weekDates)
  const editedMap = new Map(edited.filter((e) => week.has(e.date)).map((e) => [e.date, e.type]))
  const existingMap = new Map(existing.filter((e) => week.has(e.date)).map((e) => [e.date, e.type]))

  const upserts: PlanEntry[] = []
  for (const [date, type] of editedMap) {
    if (existingMap.get(date) !== type) upserts.push({ date, type })
  }
  const deletes: string[] = []
  for (const date of existingMap.keys()) {
    if (!editedMap.has(date)) deletes.push(date)
  }
  return { upserts, deletes }
}
```

- [ ] **Step 4: Lancer le test, vérifier le succès**

Run: `npm test -- plan.test`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add app/seance/_lib/plan.ts app/seance/_lib/plan.test.ts
git commit -m "feat(plan): planDiff pure logic"
```

---

### Task 4: Routes `GET` + `POST /api/plan`

**Files:**
- Create: `app/api/plan/route.ts`

- [ ] **Step 1: Lire la doc route handler**

Lire `node_modules/next/dist/docs/` pour la signature actuelle d'un route handler App Router (export `GET`/`POST`, accès aux query params). Confirmer le pattern utilisé dans `app/api/dashboard/home/route.ts` et `app/api/seance/save/route.ts`.

- [ ] **Step 2: Écrire la route**

```ts
// app/api/plan/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@clerk/nextjs/server'
import { createSupabaseServer } from '../../lib/supabase-server'
import { planDiff, type PlanEntry } from '../../seance/_lib/plan'
import { weekDatesFrom } from '../../seance/_lib/helpers'

const VALID = new Set(['push', 'pull', 'legs', 'full', 'upper', 'core', 'athletics'])

export async function GET(req: NextRequest) {
  const { userId, getToken } = await auth()
  if (!userId) return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })
  const token = await getToken()
  if (!token) return NextResponse.json({ error: 'Token indisponible' }, { status: 401 })

  const weekStart = req.nextUrl.searchParams.get('weekStart')
  if (!weekStart || !/^\d{4}-\d{2}-\d{2}$/.test(weekStart)) {
    return NextResponse.json({ error: 'weekStart invalide' }, { status: 400 })
  }
  const dates = weekDatesFrom(weekStart)
  const supabase = createSupabaseServer(token)
  const { data, error } = await supabase
    .from('planned_sessions')
    .select('date, type')
    .gte('date', dates[0])
    .lte('date', dates[6])
    .order('date', { ascending: true })
  if (error) return NextResponse.json({ error: error.message }, { status: 500 })
  return NextResponse.json({ entries: data ?? [] })
}

export async function POST(req: NextRequest) {
  const { userId, getToken } = await auth()
  if (!userId) return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })
  const token = await getToken()
  if (!token) return NextResponse.json({ error: 'Token indisponible' }, { status: 401 })

  let body: { weekStart?: string; entries?: PlanEntry[] }
  try {
    body = await req.json()
  } catch {
    return NextResponse.json({ error: 'JSON invalide' }, { status: 400 })
  }
  const { weekStart, entries } = body
  if (!weekStart || !/^\d{4}-\d{2}-\d{2}$/.test(weekStart) || !Array.isArray(entries)) {
    return NextResponse.json({ error: 'Payload invalide' }, { status: 400 })
  }
  if (entries.some((e) => !VALID.has(e.type) || !/^\d{4}-\d{2}-\d{2}$/.test(e.date))) {
    return NextResponse.json({ error: 'Entrée invalide' }, { status: 400 })
  }

  const dates = weekDatesFrom(weekStart)
  const supabase = createSupabaseServer(token)

  const { data: existing, error: exErr } = await supabase
    .from('planned_sessions')
    .select('date, type')
    .gte('date', dates[0])
    .lte('date', dates[6])
  if (exErr) return NextResponse.json({ error: exErr.message }, { status: 500 })

  const { upserts, deletes } = planDiff(dates, (existing ?? []) as PlanEntry[], entries)

  if (upserts.length) {
    const rows = upserts.map((u) => ({ user_id: userId, date: u.date, type: u.type }))
    const { error } = await supabase
      .from('planned_sessions')
      .upsert(rows, { onConflict: 'user_id,date' })
    if (error) return NextResponse.json({ error: error.message }, { status: 500 })
  }
  if (deletes.length) {
    const { error } = await supabase.from('planned_sessions').delete().in('date', deletes)
    if (error) return NextResponse.json({ error: error.message }, { status: 500 })
  }
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 3: Vérifier manuellement**

Run: `npm run dev`. Connecté, en console navigateur :
```js
await fetch('/api/plan', { method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify({ weekStart:'2026-06-08', entries:[{date:'2026-06-08',type:'push'}] }) }).then(r=>r.json())
// → { ok: true }
await fetch('/api/plan?weekStart=2026-06-08').then(r=>r.json())
// → { entries: [{ date:'2026-06-08', type:'push' }] }
```

- [ ] **Step 4: Commit**

```bash
git add app/api/plan/route.ts
git commit -m "feat(api): GET/POST /api/plan with batch diff"
```

---

### Task 5: Hook `usePlan` (cache SWR)

**Files:**
- Create: `app/seance/_lib/usePlan.ts`

- [ ] **Step 1: Écrire le hook**

```ts
// app/seance/_lib/usePlan.ts
'use client'

import { createCachedResource } from './createCachedResource'
import type { PlanEntry } from './plan'

const resource = createCachedResource<{ entries: PlanEntry[] }>({
  storageKey: 'tcp:plan:v1',
  staleMs: 3 * 60 * 1000,
  fetcher: (weekStart) =>
    fetch(`/api/plan?weekStart=${weekStart}`).then((r) => {
      if (!r.ok) throw new Error('Échec chargement du plan')
      return r.json()
    }),
})

// À appeler après enregistrement du modal (purge SWR de la semaine éditée).
export const invalidatePlan = resource.invalidate

export function usePlan(weekStart: string): {
  entries: PlanEntry[]
  loading: boolean
  error: string | null
} {
  const { data, loading, error } = resource.useResource(weekStart)
  return { entries: data?.entries ?? [], loading, error }
}
```

- [ ] **Step 2: Vérifier le typecheck**

Run: `npx tsc --noEmit`
Expected: aucune nouvelle erreur.

- [ ] **Step 3: Commit**

```bash
git add app/seance/_lib/usePlan.ts
git commit -m "feat(plan): usePlan cached resource hook"
```

---

## Palier B — UI de planification (modal + chip StatusSpot)

### Task 6: Logique `buildIdleItems` + intégration `StatusSpot`

**Files:**
- Modify: `app/seance/_lib/plan.ts` (ajout)
- Test: `app/seance/_lib/plan.test.ts` (ajout)
- Modify: `app/seance/_components/StatusSpot.tsx`

- [ ] **Step 1: Ajouter le test qui échoue**

Ajouter dans `app/seance/_lib/plan.test.ts` :

```ts
import { buildIdleItems, TYPE_LABELS } from './plan'

describe('buildIdleItems', () => {
  it('sans séance du jour : greeting + Planifier', () => {
    expect(buildIdleItems('Bonjour', null)).toEqual(['Bonjour', 'Planifier ma semaine'])
  })
  it('avec séance du jour : remplace le greeting par le rappel + Ma semaine', () => {
    expect(buildIdleItems('Bonjour', TYPE_LABELS.push)).toEqual([
      "Aujourd'hui : Push",
      'Ma semaine',
    ])
  })
})
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `npm test -- plan.test`
Expected: FAIL (`buildIdleItems`/`TYPE_LABELS` non exportés).

- [ ] **Step 3: Implémenter dans `plan.ts`**

Ajouter à `app/seance/_lib/plan.ts` :

```ts
// Libellés affichables des types (alignés sur WORKOUT_TYPES + athlétisme).
export const TYPE_LABELS: Record<string, string> = {
  push: 'Push',
  pull: 'Pull',
  legs: 'Jambes',
  full: 'Full body',
  upper: 'Upper',
  core: 'Abdos',
  athletics: 'Athlétisme',
}

// Items du StatusSpot hors alertes : si une séance est prévue ce jour, le
// greeting cède la place au rappel ; sinon greeting + entrée de planif.
export function buildIdleItems(greeting: string, plannedLabel: string | null): string[] {
  if (plannedLabel) return [`Aujourd'hui : ${plannedLabel}`, 'Ma semaine']
  return [greeting, 'Planifier ma semaine']
}
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `npm test -- plan.test`
Expected: PASS.

- [ ] **Step 5: Intégrer dans `StatusSpot.tsx`**

Dans `app/seance/_components/StatusSpot.tsx` :

1. Imports en tête :
```ts
import { usePlan } from '../_lib/usePlan'
import { buildIdleItems, TYPE_LABELS } from '../_lib/plan'
import { isoToday, weekStartMonday } from '../_lib/profile'
import { PlanWeekModal } from './PlanWeekModal'
```
> Si `isoToday` n'existe pas dans `profile.ts`, l'ajouter : `export const isoToday = () => new Intl.DateTimeFormat('en-CA').format(new Date())` (date locale appareil, pour l'UI).

2. Étendre l'état `open` pour inclure la planif :
```ts
const [open, setOpen] = useState<AlertKind | 'plan' | null>(null)
```

3. Après les hooks existants, dériver la séance du jour :
```ts
const weekStart = mounted ? isoToday(weekStartMonday()) : ''
const { entries } = usePlan(weekStart)
const todayIso = mounted ? isoToday() : ''
const plannedToday = entries.find((e) => e.date === todayIso) ?? null
const plannedLabel = plannedToday ? TYPE_LABELS[plannedToday.type] ?? null : null
```
> `weekStartMonday()` rend un `Date` → adapter `isoToday(d?)` pour accepter une `Date` optionnelle : `export const isoToday = (d: Date = new Date()) => new Intl.DateTimeFormat('en-CA').format(d)`.

4. Remplacer le calcul de `items` (la ligne `const items = alertActive ? ... : [base]`) par :
```ts
const items = alertActive ? alerts.map((a) => a.label) : buildIdleItems(base, plannedLabel)
```

5. Rendre la pill cliquable quand elle porte la planif (pas seulement les alertes). Là où la pill gère `onClick`/`role` (actuellement conditionné à `alertActive`), ajouter le cas planif : si `!alertActive`, un clic ouvre `setOpen('plan')`. Concrètement, définir :
```ts
const planMode = !alertActive
const interactive = alertActive || planMode
const onPill = () => (alertActive ? openTarget() : setOpen('plan'))
```
puis utiliser `interactive`/`onPill` à la place de `alertActive`/`openTarget` sur le conteneur de la pill (role/tabIndex/onClick/onKeyDown). Le badge « ! » pulsant reste conditionné à `alertActive` **uniquement** (pas d'urgence sur la planif).

6. Monter le modal près des autres modals en fin de composant :
```tsx
<PlanWeekModal open={open === 'plan'} onClose={() => setOpen(null)} />
```

- [ ] **Step 6: Vérifier manuellement (après Task 7)**

La vérification visuelle se fait à la Task 7 une fois le modal créé. Pour l'instant : `npx tsc --noEmit` ne doit pas régresser (le modal sera importé à la Task 7 — committer Task 6 et 7 ensemble si l'import bloque le typecheck).

- [ ] **Step 7: Commit**

```bash
git add app/seance/_lib/plan.ts app/seance/_lib/plan.test.ts app/seance/_components/StatusSpot.tsx app/seance/_lib/profile.ts
git commit -m "feat(plan): buildIdleItems + StatusSpot planif chip"
```

---

### Task 7: `PlanWeekModal`

**Files:**
- Create: `app/seance/_components/PlanWeekModal.tsx`

- [ ] **Step 1: Lire un modal existant pour le style**

Lire `app/seance/_components/WeightModal.tsx` pour reprendre exactement le pattern de modal (overlay, scrim, animation d'entrée, fermeture, portail éventuel) et les primitives (`Button`, etc.).

- [ ] **Step 2: Implémenter le modal**

Créer `app/seance/_components/PlanWeekModal.tsx`. Structure (suivre le style du modal existant, classes/couleurs du design system) :

```tsx
'use client'

import { useMemo, useState } from 'react'
import { WORKOUT_TYPES } from '../_lib/constants'
import { TYPE_LABELS } from '../_lib/plan'
import { usePlan, invalidatePlan } from '../_lib/usePlan'
import { weekDatesFrom } from '../_lib/helpers'
import { weekStartMonday } from '../_lib/profile'
import { isoToday } from '../_lib/profile'

const DAY_NAMES = ['Lun', 'Mar', 'Mer', 'Jeu', 'Ven', 'Sam', 'Dim']
const TYPE_CHOICES = [...WORKOUT_TYPES.map((t) => t.id), 'athletics'] as const

type Props = { open: boolean; onClose: () => void }

export function PlanWeekModal({ open, onClose }: Props) {
  const weekStart = isoToday(weekStartMonday())
  const dates = useMemo(() => weekDatesFrom(weekStart), [weekStart])
  const { entries } = usePlan(weekStart)

  // État local éditable, initialisé depuis le plan chargé.
  const [draft, setDraft] = useState<Record<string, string>>(() =>
    Object.fromEntries(entries.map((e) => [e.date, e.type])),
  )
  const [editing, setEditing] = useState<string | null>(null) // date dont on choisit le type
  const [saving, setSaving] = useState(false)

  const setType = (date: string, type: string | null) => {
    setDraft((d) => {
      const next = { ...d }
      if (type === null) delete next[date]
      else next[date] = type
      return next
    })
    setEditing(null)
  }

  const save = async () => {
    setSaving(true)
    const payload = {
      weekStart,
      entries: Object.entries(draft).map(([date, type]) => ({ date, type })),
    }
    try {
      const r = await fetch('/api/plan', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify(payload),
      })
      if (!r.ok) throw new Error('save')
      invalidatePlan(weekStart)
      onClose()
      // Hook d'abonnement push branché à la Task 12.
    } finally {
      setSaving(false)
    }
  }

  if (!open) return null

  return (
    // Reprendre l'enveloppe overlay/scrim/animation de WeightModal.
    // Contenu : titre « Planifier ma semaine », sous-titre, liste des 7 jours.
    // Chaque jour : DAY_NAMES[i] + date courte + pill du type (TYPE_LABELS) ou « + Ajouter ».
    //   - clic sur un jour → setEditing(date) → affiche le choix : TYPE_CHOICES (vert muscu /
    //     orange athlé) + « Aucune » (→ setType(date, null)).
    // Bouton « Enregistrer » (disabled si saving) → save().
    // Couleurs : muscu = var(--accent) (vert), athlé = var(--warn) (orange), chrome = var(--brand).
    <div /* … overlay … */>{/* … */}</div>
  )
}
```

> Le bloc JSX d'enveloppe/lignes est volontairement laissé à reproduire **à l'identique du style de `WeightModal`** (mêmes tokens, mêmes animations). Aucune logique nouvelle au-delà de ce qui est ci-dessus.

- [ ] **Step 3: Vérifier manuellement**

Run: `npm run dev`. Sur `/seance` : la pill du hero affiche « Planifier ma semaine » dans son défilé ; un clic ouvre le modal ; assigner des types, « Enregistrer », rouvrir → les types persistent. Recharger la page → toujours là.

- [ ] **Step 4: Commit**

```bash
git add app/seance/_components/PlanWeekModal.tsx
git commit -m "feat(plan): PlanWeekModal minimal weekly planner"
```

---

## Palier C — Jour J : pré-sélection + split-button

### Task 8: `NavContext.plannedType` → ConfigScreen pré-rempli

**Files:**
- Modify: `app/seance/_lib/types.ts` (type `NavContext`)
- Modify: `app/seance/SessionClient.tsx`

- [ ] **Step 1: Étendre `NavContext`**

Dans `app/seance/_lib/types.ts`, ajouter à `NavContext` :
```ts
  // Type pré-rempli depuis une séance planifiée : ouvre ConfigScreen au chrono.
  plannedType?: string
```

- [ ] **Step 2: Câbler dans `SessionClient.nav`**

Dans `app/seance/SessionClient.tsx`, dans la fonction `nav` (vers `SessionClient.tsx:107`), avant `setStep(s)` :
```ts
if (ctx?.plannedType) {
  setSession((prev) => ({ ...prev, type: ctx.plannedType! }))
  setConfigInitialStep('chrono')
}
```
> `ConfigScreen` lit déjà `session.type` (`useState(session.type || 'push')`) et `initialStep` → il s'ouvrira sur le chrono avec le bon type. Le retour « ← » depuis le chrono ramène au sélecteur de type (override conservé). Aucune autre modif de `ConfigScreen`.

- [ ] **Step 3: Vérifier le typecheck**

Run: `npx tsc --noEmit`
Expected: pas de nouvelle erreur.

- [ ] **Step 4: Commit**

```bash
git add app/seance/_lib/types.ts app/seance/SessionClient.tsx
git commit -m "feat(plan): nav plannedType opens config at chrono"
```

---

### Task 9: Split-button jour J dans `IdleScreen`

**Files:**
- Modify: `app/seance/_screens/IdleScreen.tsx`

- [ ] **Step 1: Dériver `plannedToday` dans l'IdleScreen**

En tête du composant `IdleScreen` (après les hooks existants ~`IdleScreen.tsx:31`) :
```ts
import { usePlan } from '../_lib/usePlan'
import { TYPE_LABELS } from '../_lib/plan'
import { weekStartMonday, isoToday } from '../_lib/profile'
// …
const { entries: planEntries } = usePlan(mounted ? isoToday(weekStartMonday()) : '')
const plannedToday = mounted ? planEntries.find((e) => e.date === isoToday()) ?? null : null
const plannedLabel = plannedToday ? TYPE_LABELS[plannedToday.type] ?? null : null
```

- [ ] **Step 2: Remplacer le CTA fixe par un split-button conditionnel**

Localiser le bouton du bas (`IdleScreen.tsx:577`, `<Button size="lg" onClick={() => nav('config')} …>Commencer une séance</Button>`). Le remplacer par :
```tsx
{plannedToday ? (
  <SplitStartButton
    label={plannedLabel ?? 'Push'}
    onStart={() => nav('config', { plannedType: plannedToday.type })}
    onFree={() => nav('config')}
  />
) : (
  <Button size="lg" onClick={() => nav('config')} icon={<Dumbbell size={16} />} gpu style={{ boxShadow: 'none' }}>
    Commencer une séance
  </Button>
)}
```

- [ ] **Step 3: Implémenter `SplitStartButton` en bas du fichier**

Ajouter le composant (mêmes tokens que `Button`/`primitives` ; `useReducedMotion` pour le menu) :
```tsx
function SplitStartButton({
  label,
  onStart,
  onFree,
}: {
  label: string
  onStart: () => void
  onFree: () => void
}) {
  const [openMenu, setOpenMenu] = useState(false)
  const reduce = useReducedMotion()
  return (
    <div style={{ position: 'relative' }}>
      {openMenu && (
        <>
          {/* scrim invisible pour fermer au tap-dehors */}
          <div onClick={() => setOpenMenu(false)} style={{ position: 'fixed', inset: 0, zIndex: 1 }} />
          <motion.div
            role="menu"
            initial={reduce ? false : { opacity: 0, y: 8 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.2, ease: EASE.out }}
            style={{
              position: 'absolute',
              bottom: 'calc(100% + 10px)',
              left: 0,
              right: 0,
              zIndex: 2,
              background: 'var(--glass-strong)',
              backdropFilter: 'blur(18px)',
              WebkitBackdropFilter: 'blur(18px)',
              border: '1px solid var(--hairline)',
              borderRadius: 16,
              padding: 7,
            }}
          >
            <button
              role="menuitem"
              onClick={() => {
                setOpenMenu(false)
                onFree()
              }}
              style={{
                width: '100%',
                appearance: 'none',
                border: 'none',
                background: 'transparent',
                color: 'var(--ink)',
                fontFamily: 'var(--font)',
                fontWeight: 600,
                fontSize: 14,
                textAlign: 'left',
                padding: '13px 12px',
                borderRadius: 11,
                cursor: 'pointer',
              }}
            >
              Séance libre
            </button>
          </motion.div>
        </>
      )}
      <div style={{ display: 'flex', borderRadius: 'var(--radius-full)', overflow: 'hidden' }}>
        <Button
          size="lg"
          onClick={onStart}
          gpu
          style={{ flex: 1, borderRadius: 0, boxShadow: 'none' }}
        >
          Commencer {label}
        </Button>
        <button
          aria-haspopup="menu"
          aria-expanded={openMenu}
          aria-label="Autre séance"
          onClick={() => setOpenMenu((v) => !v)}
          style={{
            width: 54,
            border: 'none',
            cursor: 'pointer',
            background: 'var(--brand)',
            color: 'var(--brand-ink)',
            borderLeft: '1px solid color-mix(in oklch, black 18%, transparent)',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          <ChevronRight size={18} style={{ transform: openMenu ? 'rotate(-90deg)' : 'rotate(-90deg)' }} />
        </button>
      </div>
    </div>
  )
}
```
> Icône : réutiliser un chevron de `icons.tsx` ; orienter vers le haut (`rotate(-90deg)` sur `ChevronRight`, ou importer un `ChevronUp`/`ChevronDown` s'il existe). Fermeture clavier : ajouter `onKeyDown` Échap sur le conteneur si besoin (cf. pattern `StatusSpot`).

- [ ] **Step 4: Vérifier manuellement**

Run: `npm run dev`. Planifier une séance **aujourd'hui** via le modal. Recharger `/seance` : le CTA du bas devient « Commencer {type} » ; le clic principal ouvre `config` directement au **chrono** avec le bon type ; la flèche ouvre « Séance libre » → `config` à l'étape **type**. Sans séance ce jour : CTA « Commencer une séance » normal.

- [ ] **Step 5: Commit**

```bash
git add app/seance/_screens/IdleScreen.tsx
git commit -m "feat(plan): day-of split-button CTA with Séance libre menu"
```

---

## Palier D — Web Push

### Task 10: Dépendance `web-push`, VAPID, abonnement client + `/api/push/subscribe`

**Files:**
- Modify: `package.json` (via npm)
- Create: `app/seance/_lib/push.ts`
- Create: `app/api/push/subscribe/route.ts`

- [ ] **Step 1: Installer `web-push` et générer les clés VAPID**

```bash
npm install web-push
npx web-push generate-vapid-keys
```
Reporter les clés en env (`.env` local + dashboard Vercel) :
```
NEXT_PUBLIC_VAPID_PUBLIC_KEY=<public>
VAPID_PRIVATE_KEY=<private>
VAPID_SUBJECT=mailto:g@growthist.io
```

- [ ] **Step 2: Helper d'abonnement client `push.ts`**

```ts
// app/seance/_lib/push.ts
'use client'

function urlBase64ToUint8Array(base64: string): Uint8Array {
  const padding = '='.repeat((4 - (base64.length % 4)) % 4)
  const b64 = (base64 + padding).replace(/-/g, '+').replace(/_/g, '/')
  const raw = atob(b64)
  return Uint8Array.from([...raw].map((c) => c.charCodeAt(0)))
}

export function pushSupported(): boolean {
  return (
    typeof window !== 'undefined' &&
    'serviceWorker' in navigator &&
    'PushManager' in window &&
    'Notification' in window
  )
}

// iOS : le push n'existe que si la PWA est installée (standalone).
export function isStandalone(): boolean {
  if (typeof window === 'undefined') return false
  return (
    window.matchMedia('(display-mode: standalone)').matches ||
    (navigator as unknown as { standalone?: boolean }).standalone === true
  )
}

// Demande la permission puis enregistre l'abonnement. Renvoie true si abonné.
export async function subscribeToPush(): Promise<boolean> {
  if (!pushSupported()) return false
  const perm = await Notification.requestPermission()
  if (perm !== 'granted') return false

  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.subscribe({
    userVisibleOnly: true,
    applicationServerKey: urlBase64ToUint8Array(process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY!),
  })
  const json = sub.toJSON()
  const r = await fetch('/api/push/subscribe', {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({
      endpoint: json.endpoint,
      p256dh: json.keys?.p256dh,
      auth: json.keys?.auth,
    }),
  })
  return r.ok
}
```

- [ ] **Step 3: Route `/api/push/subscribe`**

```ts
// app/api/push/subscribe/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@clerk/nextjs/server'
import { createSupabaseServer } from '../../../lib/supabase-server'

export async function POST(req: NextRequest) {
  const { userId, getToken } = await auth()
  if (!userId) return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })
  const token = await getToken()
  if (!token) return NextResponse.json({ error: 'Token indisponible' }, { status: 401 })

  let body: { endpoint?: string; p256dh?: string; auth?: string }
  try {
    body = await req.json()
  } catch {
    return NextResponse.json({ error: 'JSON invalide' }, { status: 400 })
  }
  if (!body.endpoint || !body.p256dh || !body.auth) {
    return NextResponse.json({ error: 'Abonnement incomplet' }, { status: 400 })
  }

  const supabase = createSupabaseServer(token)
  const { error } = await supabase
    .from('push_subscriptions')
    .upsert(
      { user_id: userId, endpoint: body.endpoint, p256dh: body.p256dh, auth: body.auth },
      { onConflict: 'endpoint' },
    )
  if (error) return NextResponse.json({ error: error.message }, { status: 500 })
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 4: Vérifier le typecheck**

Run: `npx tsc --noEmit`
Expected: pas de nouvelle erreur.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json app/seance/_lib/push.ts app/api/push/subscribe/route.ts
git commit -m "feat(push): web-push dep, client subscribe helper + subscribe route"
```

---

### Task 11: Handlers `push` + `notificationclick` dans le service worker

**Files:**
- Modify: `public/sw.js`

- [ ] **Step 1: Ajouter les handlers et bumper la version du cache**

Dans `public/sw.js` : passer `CACHE_VERSION`/`RUNTIME` de `tpc-v3`/`tpc-runtime-v3` à `tpc-v4`/`tpc-runtime-v4`, puis ajouter à la fin :
```js
// Web Push : réveille le SW même app fermée.
self.addEventListener('push', (event) => {
  let data = {}
  try {
    data = event.data ? event.data.json() : {}
  } catch (e) {
    data = {}
  }
  const title = data.title || 'Séance du jour'
  const options = {
    body: data.body || "On y va ? Ta séance t'attend.",
    icon: '/icon-192.png',
    badge: '/icon-192.png',
    data: { url: data.url || '/seance' },
  }
  event.waitUntil(self.registration.showNotification(title, options))
})

self.addEventListener('notificationclick', (event) => {
  event.notification.close()
  const url = (event.notification.data && event.notification.data.url) || '/seance'
  event.waitUntil(
    self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then((list) => {
      for (const c of list) {
        if (c.url.includes(url) && 'focus' in c) return c.focus()
      }
      return self.clients.openWindow(url)
    }),
  )
})
```

- [ ] **Step 2: Vérifier manuellement (DevTools)**

`npm run build && npm start` (le SW ne s'enregistre qu'en prod). Sur l'app : DevTools → Application → Service Workers → confirmer `sw.js` activé en `v4`. Onglet Push : envoyer un push de test avec payload `{"title":"Test","body":"Hello","url":"/seance"}` → la notification s'affiche ; le clic ouvre `/seance`.

- [ ] **Step 3: Commit**

```bash
git add public/sw.js
git commit -m "feat(push): sw push + notificationclick handlers, cache v4"
```

---

### Task 12: Demander l'abonnement après le 1er enregistrement de plan

**Files:**
- Modify: `app/seance/_components/PlanWeekModal.tsx`

- [ ] **Step 1: Brancher l'abonnement dans `save()`**

Dans `PlanWeekModal.tsx`, importer :
```ts
import { subscribeToPush, pushSupported, isStandalone } from '../_lib/push'
```
Puis, dans `save()`, après `invalidatePlan(weekStart)` et avant `onClose()` :
```ts
// Au premier plan enregistré, proposer les notifs (une seule fois).
if (pushSupported() && Notification.permission === 'default') {
  const onIOS = /iphone|ipad|ipod/i.test(navigator.userAgent)
  if (!onIOS || isStandalone()) {
    await subscribeToPush()
  }
  // iOS non installé : ne pas demander ; l'indice d'installation est géré ailleurs (voir note).
}
```
> Note iOS : si `onIOS && !isStandalone()`, afficher un indice « Ajoute à l'écran d'accueil pour activer les rappels » (petit texte sous le bouton Enregistrer, non bloquant). Le rendre via un `useState` local `showIosHint` posé dans ce même bloc.

- [ ] **Step 2: Vérifier manuellement**

En prod local (`npm run build && npm start`), installer la PWA / ou sur desktop Chrome : enregistrer un plan → le navigateur demande la permission de notification → accepter → vérifier en DB (`push_subscriptions`) qu'une ligne existe.

- [ ] **Step 3: Commit**

```bash
git add app/seance/_components/PlanWeekModal.tsx
git commit -m "feat(push): request subscription after first plan save"
```

---

## Palier E — Planificateur (cron) + envoi serveur

### Task 13: Logique pure `selectNotifiable`

**Files:**
- Modify: `app/seance/_lib/plan.ts` (ajout)
- Test: `app/seance/_lib/plan.test.ts` (ajout)

- [ ] **Step 1: Ajouter le test qui échoue**

Ajouter dans `plan.test.ts` :
```ts
import { selectNotifiable } from './plan'

describe('selectNotifiable', () => {
  it('joint plans et abonnements par user et construit le message', () => {
    const plans = [{ user_id: 'u1', type: 'push' }, { user_id: 'u2', type: 'athletics' }]
    const subs = [
      { user_id: 'u1', endpoint: 'e1', p256dh: 'k1', auth: 'a1' },
      { user_id: 'u3', endpoint: 'e3', p256dh: 'k3', auth: 'a3' }, // pas de plan → ignoré
    ]
    const out = selectNotifiable(plans, subs)
    expect(out).toHaveLength(1)
    expect(out[0].sub.endpoint).toBe('e1')
    expect(out[0].payload.title).toContain('Push')
    expect(out[0].payload.url).toBe('/seance')
  })
})
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `npm test -- plan.test`
Expected: FAIL (`selectNotifiable` non exporté).

- [ ] **Step 3: Implémenter dans `plan.ts`**

```ts
type PlanRow = { user_id: string; type: string }
type SubRow = { user_id: string; endpoint: string; p256dh: string; auth: string }
export type PushTask = {
  sub: { endpoint: string; keys: { p256dh: string; auth: string } }
  payload: { title: string; body: string; url: string }
}

// Jointure pure plans×abonnements : pour chaque abonnement dont l'utilisateur a
// une séance ce jour, produit la tâche d'envoi (message + clés).
export function selectNotifiable(plans: PlanRow[], subs: SubRow[]): PushTask[] {
  const typeByUser = new Map(plans.map((p) => [p.user_id, p.type]))
  const tasks: PushTask[] = []
  for (const s of subs) {
    const type = typeByUser.get(s.user_id)
    if (!type) continue
    const label = TYPE_LABELS[type] ?? type
    tasks.push({
      sub: { endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } },
      payload: { title: `Séance ${label} aujourd'hui`, body: "On y va ? Ta séance t'attend.", url: '/seance' },
    })
  }
  return tasks
}
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `npm test -- plan.test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/seance/_lib/plan.ts app/seance/_lib/plan.test.ts
git commit -m "feat(push): selectNotifiable pure join logic"
```

---

### Task 14: Client admin Supabase + route `/api/cron/notify`

**Files:**
- Modify: `app/lib/supabase-server.ts`
- Create: `app/api/cron/notify/route.ts`

- [ ] **Step 1: Ajouter `createSupabaseAdmin`**

Dans `app/lib/supabase-server.ts`, ajouter :
```ts
// Client admin (clé secrète) — bypass RLS. Réservé au cron serveur.
export function createSupabaseAdmin(): SupabaseClient {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
  const key = process.env.SUPABASE_SECRET_KEY
  if (!url || !key) throw new Error('Supabase admin: URL ou SUPABASE_SECRET_KEY manquante')
  return createClient(url.trim(), key.trim(), {
    auth: { persistSession: false, autoRefreshToken: false },
  })
}
```
Ajouter `SUPABASE_SECRET_KEY=<sb_secret_…>` en env (local + Vercel).

- [ ] **Step 2: Écrire la route cron**

```ts
// app/api/cron/notify/route.ts
import { NextRequest, NextResponse } from 'next/server'
import webpush from 'web-push'
import { createSupabaseAdmin } from '../../../lib/supabase-server'
import { selectNotifiable } from '../../../seance/_lib/plan'
import { isoDateInParis } from '../../../seance/_lib/helpers'

export const runtime = 'nodejs'

export async function GET(req: NextRequest) {
  // Auth cron : Vercel injecte `Authorization: Bearer ${CRON_SECRET}`.
  const authz = req.headers.get('authorization')
  if (authz !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Interdit' }, { status: 401 })
  }

  webpush.setVapidDetails(
    process.env.VAPID_SUBJECT!,
    process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY!,
    process.env.VAPID_PRIVATE_KEY!,
  )

  const supabase = createSupabaseAdmin()
  const today = isoDateInParis(new Date())

  const { data: plans, error: pErr } = await supabase
    .from('planned_sessions')
    .select('user_id, type')
    .eq('date', today)
  if (pErr) return NextResponse.json({ error: pErr.message }, { status: 500 })
  if (!plans || plans.length === 0) return NextResponse.json({ sent: 0 })

  const userIds = [...new Set(plans.map((p) => p.user_id))]
  const { data: subs, error: sErr } = await supabase
    .from('push_subscriptions')
    .select('user_id, endpoint, p256dh, auth')
    .in('user_id', userIds)
  if (sErr) return NextResponse.json({ error: sErr.message }, { status: 500 })

  const tasks = selectNotifiable(plans, subs ?? [])
  let sent = 0
  const dead: string[] = []
  for (const t of tasks) {
    try {
      await webpush.sendNotification(
        { endpoint: t.sub.endpoint, keys: t.sub.keys },
        JSON.stringify(t.payload),
      )
      sent++
    } catch (e: unknown) {
      const code = (e as { statusCode?: number })?.statusCode
      if (code === 404 || code === 410) dead.push(t.sub.endpoint)
    }
  }
  if (dead.length) {
    await supabase.from('push_subscriptions').delete().in('endpoint', dead)
  }
  return NextResponse.json({ sent, removed: dead.length })
}
```

- [ ] **Step 3: Vérifier manuellement**

Avec un plan pour aujourd'hui + un abonnement en DB et `CRON_SECRET` en env local :
```bash
curl -H "Authorization: Bearer <CRON_SECRET>" http://localhost:3000/api/cron/notify
# → {"sent":1,"removed":0} et la notification arrive sur l'appareil abonné
```

- [ ] **Step 4: Commit**

```bash
git add app/lib/supabase-server.ts app/api/cron/notify/route.ts
git commit -m "feat(push): cron notify route + supabase admin client"
```

---

### Task 15: Cron Vercel + checklist de déploiement

**Files:**
- Create: `vercel.json`

- [ ] **Step 1: Déclarer le cron**

```json
{
  "crons": [{ "path": "/api/cron/notify", "schedule": "0 6 * * *" }]
}
```
> `0 6 * * *` UTC ≈ 08:00–08:59 Europe/Paris en été (CEST), 07:00–07:59 en hiver (CET). Hobby = 1 cron/jour, déclenché dans le créneau d'1h.

- [ ] **Step 2: Vérifier la présence des env de prod sur Vercel**

S'assurer que le dashboard Vercel contient : `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `SUPABASE_SECRET_KEY`, `CRON_SECRET` (+ les clés Supabase/Clerk existantes).

- [ ] **Step 3: Déployer et tester le cron**

Déployer. Dans Vercel → Settings → Cron Jobs : déclencher manuellement `/api/cron/notify` une fois, vérifier la réponse `{ sent, removed }` et la réception de la notification.

- [ ] **Step 4: Commit**

```bash
git add vercel.json
git commit -m "feat(push): vercel daily cron for notifications"
```

---

## Self-Review (couverture spec)

- §4 modèle de données → Task 1. ✅
- §5.1 `usePlan` → Task 5. ✅
- §5.2 chip `StatusSpot` (défilé, retrait greeting, pas de badge) → Task 6. ✅
- §5.3 `PlanWeekModal` (batch-save, invalidate) → Task 7 (+ abonnement après 1er save → Task 12). ✅
- §5.4 split-button jour J + « Séance libre » → Task 9. ✅
- §5.5 `NavContext.plannedType` + chrono pré-rempli → Task 8. ✅
- §5.6 push SW + `push.ts` + garde iOS → Tasks 10, 11, 12. ✅
- §5.7 routes plan / subscribe / cron → Tasks 4, 10, 14. ✅
- §5.8 cron Vercel → Task 15. ✅
- §5.9 client admin → Task 14. ✅
- §7 cas limites (permission, iOS, dead subs, no sub) → Tasks 12, 14. ✅
- §8 tests (dates, buildIdleItems, selectNotifiable, planDiff) → Tasks 2, 3, 6, 13. ✅
- §9 prérequis env/VAPID/migration/vercel.json → Tasks 1, 10, 14, 15. ✅

Toutes les sections de la spec sont couvertes par au moins une tâche.
